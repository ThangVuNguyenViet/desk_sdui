name: desk_sdui_demo
description: Reference + parity tests for desk_sdui.
version: 0.0.1-dev
publish_to: none

environment:
  sdk: ^3.7.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  desk_sdui:
    path: ../desk_sdui
  desk_sdui_annotation:
    path: ../desk_sdui_annotation
  cue: ^0.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  desk_sdui_generator:
    path: ../desk_sdui_generator
  build_runner: ^2.4.0
  very_good_analysis: ^7.0.0

dependency_overrides:
  # Temporary: analyzer ^13 needs meta ^1.18; stable Flutter pins 1.17. Drop when Flutter SDK catches up.
  meta: ^1.18.0
